// Pseudocode

// sort lost and reserve
// find available sets
    // remove number in both reserve and lost
// lend reserve to peers
    // if the different between lost and reserve is 1 apart, lent
    // if lent, remove number from both lost and reserve
// return total number of participants in gym class

function solution(n, lost, reserve) {
    var answer = 0;
    return answer;
}